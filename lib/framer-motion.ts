// Import the necessary parts from framer-motion
// This is a lightweight wrapper to avoid directly importing the full library
import {
  motion,
  AnimatePresence,
  useAnimation,
  useInView,
  useScroll,
  useTransform,
  type Variant
} from "framer-motion";

// Re-export the imported components and hooks
export {
  motion,
  AnimatePresence,
  useAnimation,
  useInView,
  useScroll,
  useTransform,
  type Variant
};